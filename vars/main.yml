---
influxdb_url: "{{ source.influxdb_url }}"
influxdb_config_options:
  assets-path: "{{ influxdb_assets_path | default(omit) }}"
  bolt-path: "{{ influxdb_bolt_path | default(omit) }}"
  e2e-testing: "{{ influxdb_e2e_testing | default(omit) }}"
  engine-path: "{{ influxdb_engine_path | default(omit) }}"
  http-bind-address: "{{ influxdb_http_bind_address | default(omit) }}"
  influxql-max-select-buckets: "{{ influxdb_influxql_max_select_buckets | default(omit) }}"
  influxql-max-select-point: "{{ influxdb_influxql_max_select_point | default(omit) }}"
  influxql-max-select-series: "{{ influxdb_influxql_max_select_series | default(omit) }}"
  log-level: "{{ influxdb_log_level | default(omit) }}"
  new-meta-store: "{{ influxdb_new_meta_store | default(omit) }}"
  new-meta-store-read-only: "{{ influxdb_new_meta_store_read_only | default(omit) }}"
  no-tasks: "{{ influxdb_no_tasks | default(omit) }}"
  query-concurrency: "{{ influxdb_query_concurrency | default(omit) }}"
  query-initial-memory-bytes: "{{ influxdb_query_initial_memory_bytes | default(omit) }}"
  query-max-memory-bytes: "{{ influxdb_query_max_memory_bytes | default(omit) }}"
  query-memory-bytes: "{{ influxdb_query_memory_bytes | default(omit) }}"
  query-queue-size: "{{ influxdb_query_queue_size | default(omit) }}"
  reporting-disabled: "{{ influxdb_reporting_disabled | default(omit) }}"
  secret-store: "{{ influxdb_secret_store | default(omit) }}"
  session-length: "{{ influxdb_session_length | default(omit) }}"
  session-renew-disabled: "{{ influxdb_session_renew_disabled | default(omit) }}"
  storage-cache-max-memory-size: "{{ influxdb_storage_cache_max_memory_size | default(omit) }}"
  storage-cache-snapshot-memory-size: "{{ influxdb_storage_cache_max_memory_size | default(omit) }}"
  storage-cache-snapshot-write-cold-duration: "{{ influxdb_storage_cache_snapshot_write_cold_duration | default(omit) }}"
  storage-compact-full-write-cold-duration: "{{ influxdb_storage_compact_full_write_cold_duration | default(omit) }}"
  storage-compact-throughput-burst: "{{ influxdb_storage_compact_throughput_burst | default(omit) }}"
  storage-max-concurrent-compactions: "{{ influxdb_storage_max_concurrent_compactions | default(omit) }}"
  storage-max-index-log-file-size: "{{ influxdb_storage_max_index_log_file_size | default(omit) }}"
  storage-retention-check-interval: "{{ influxdb_storage_retention_check_interval | default(omit) }}"
  storage-series-file-max-concurrent-snapshot-compactions: "{{ influxdb_storage_series_file_max_concurrent_snapshot_compactions | default(omit) }}"
  storage-series-id-set-cache-size: "{{ influxdb_storage_series_id_set_cache_size | default(omit) }}"
  storage-shard-precreator-advance-period: "{{ influxdb_storage_shard_precreator_advance_period | default(omit) }}"
  storage-shard-precreator-check-interval: "{{ influxdb_storage_shard_precreator_check_interval | default(omit) }}"
  storage-tsm-use-madv-willneed: "{{ influxdb_storage_tsm_use_madv_willneed | default(omit) }}"
  storage-validate-keys: "{{ influxdb_storage_validate_keys | default(omit) }}"
  storage-wal-fsync-delay: "{{ influxdb_storage_wal_fsync_delay | default(omit) }}"
  store: "{{ influxdb_store | default(omit) }}"
  tls-cert: "{{ influxdb_tls_cert | default(omit) }}"
  tls-key: "{{ influxdb_tls_key | default(omit) }}"
  tls-min-version: "{{ influxdb_tls_min_version | default(omit) }}"
  tls-strict-ciphers: "{{ influxdb_tls_strict_ciphers | default(omit) }}"
  tracing-type: "{{ influxdb_tracing_type | default(omit) }}"
  vault-addr: "{{ influxdb_vault_addr | default(omit) }}"
  vault-cacert: "{{ influxdb_vault_cacert | default(omit) }}"
  vault-capath: "{{ influxdb_vault_capath | default(omit) }}"
  vault-client-cert: "{{ influxdb_vault_client_cert | default(omit) }}"
  vault-client-key: "{{ influxdb_vault_client_key | default(omit) }}"
  vault-max-retries: "{{ influxdb_vault_max_retries | default(omit) }}"
  vault-client-timeout: "{{ influxdb_vault_client_timeout | default(omit) }}"
  vault-skip-verify: "{{ influxdb_vault_skip_verify | default(omit) }}"
  vault-tls-server-name: "{{ influxdb_vault_tls_server_name | default(omit) }}"
  vault-token: "{{ influxdb_vault_token | default(omit) }}"
